chain private {
	include "./services.d/private/*.nft"

	# comment out the next line to not include public services
	return comment "return to trampoline; include public services"

	# unreachable by design if the expression above is enabled
	jump proto-filter
	goto reject-verdict
}
